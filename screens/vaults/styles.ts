import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  listContainer: {
    gap: 8,
    padding: 8,
  },
});

export default Styles;
