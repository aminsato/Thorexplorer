import { StyleSheet } from "react-native";

import { gray01 } from "@/styles";

const Styles = StyleSheet.create({
  container: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
  text: {
    color: gray01,
    fontSize: 24,
    marginBottom: 20,
  },
});

export default Styles;
